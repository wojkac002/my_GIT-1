 case 1: {
                float liczba1, liczba2, wynik;
                printf("Podaj 2 liczby do obliczenia: \n");
                scanf("%f", &liczba1);
                scanf("%f", &liczba2);
        wynik = liczba1 + liczba2;
        printf("Suma liczb = %.1f", liczba1 + liczba2);
        break;
    }
